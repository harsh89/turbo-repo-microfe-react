import React from "react";

export const ErrorBoundary = () => (
  <div style={{ background: "rgba(43, 192, 219, 0.3)" }}>
    <h1>This is error boundary</h1>
  </div>
 );
 export default ErrorBoundary;